# Google Cloud Configuration
# Required: Set your Google Cloud Project ID
GCP_PROJECT_ID=your-project-id

# Firestore Database Configuration
# Optional: Firestore database ID (default: '(default)')
# Use '(default)' for the default database or specify a custom database ID
FIRESTORE_DATABASE_ID=(default)

# Google Cloud Storage Configuration
# Required: Set your storage bucket name for file uploads
GCS_BUCKET_NAME=your-storage-bucket
STORAGE_BUCKET_NAME=your-storage-bucket
STORAGE_UPLOAD_PATH=pending

# Server Configuration
# Optional: Server port (default: 3001)
PORT=3001
NODE_ENV=development

# Firestore Collection Names (Optional - defaults shown)
# Customize these for different customer environments
COLLECTION_VENDORS=vendor_configurations
COLLECTION_EXTRACTIONS=extractionResults
COLLECTION_RECONCILIATIONS=invoice_reconciliation_summaries
COLLECTION_RULES=reconciliation_rules
COLLECTION_STATUS=reconciliation_status
COLLECTION_EXTRACTION_METADATA=extraction_metadata
COLLECTION_EXTRACTION_PARTS=extraction_parts

# Client-side Configuration
# Optional: API base URL for the frontend
VITE_API_BASE_URL=/api

# Authentication Setup
# The application uses Google Application Default Credentials (ADC)
# No API keys needed in environment variables
# Set up ADC by running: gcloud auth application-default login